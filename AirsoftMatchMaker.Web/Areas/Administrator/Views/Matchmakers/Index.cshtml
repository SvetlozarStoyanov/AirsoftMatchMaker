@using System.Text.RegularExpressions;
@model IEnumerable<MatchmakerListModel>
<table class=" table table-bordered  table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Username
            </th>
            <th>
                Organised Games
            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td width="50%">
                    @user.UserName
                </td>
                <td width="30%">
                    @user.OrganisedGames games
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Matchmakers" asp-action="Details" asp-route-id="@user.Id" class="btn btn-primary mx-2">
                            <i class="bi bi-info-square-fill"></i>
                            Details
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>

</table>
