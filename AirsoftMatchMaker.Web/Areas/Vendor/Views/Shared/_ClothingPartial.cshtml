@model ClothingListModel

@{
    string img = @Model.ImageUrl != null ? @Model.ImageUrl : $"/img/Default/Weapons/{Model.ClothingColor}.jpg";
    string altImg = $"/img/Default/Weapons/{Model.ClothingColor}.jpg";
    //string temp = "style="width: 200px; height: 200px; "";
}
<div class="col-md-4 mt-3">
    <div class="card mb-3">
        <div class="text-center">
            <img style="height:250px;" class="card-img-top" src="@img" alt="Clothing Image">
        </div>
        <div class="card-body text-center">
            <h4 class="text-center">@Model.Name</h4>
            <h6>Color: <b>@string.Join(" ", Regex.Matches(Model.ClothingColor.ToString(), "[A-Z][a-z]*").Select(m => m.Value).ToArray())</b></h6>
            <h6>Price: <b>@Model.Price credits</b></h6>
            <br />
            <a asp-area="Vendor" asp-controller="Clothes" asp-action="Details" asp-route-id="@Model.Id"
               class="btn btn-primary">
                <i class="bi bi-info"></i>
                Details
            </a>

            @*    <a asp-controller="Houses" asp-action="Delete" asp-route-id="@Model.Id"
            class="btn btn-danger">
            Delete
            </a>*@
        </div>
    </div>
</div>
