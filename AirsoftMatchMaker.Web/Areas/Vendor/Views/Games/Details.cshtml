@*@using System.Text.RegularExpressions;
@model GameViewModel

@{
    ViewBag.Title = "Game Details";
}

<h2 class="text-center">@ViewBag.Title</h2>


<div class="container" style="display:inline">
    <div class="row">

        <div class="card col-8 border-0">
            <p style="font-size:25px;">@Model.Name</p>
            <p style="font-size:25px;">@Model.Result</p>
        
            <p>
                Status:
                <b>@string.Join(" ", Regex.Matches(Model.GameStatus.ToString(), "[A-Z][a-z]*").Select(m => m.Value).ToArray())</b>
            </p>
            <p>
                Matchmaker:
                <b>
                    <a class="btn btn-info" asp-area="Vendor" asp-controller="Matchmakers" asp-action="Details" asp-route-id="@Model.MatchmakerId"> @Model.MatchmakerName</a>
                </b>
            </p>
            <p>
                Map
                <b>
                    <a class="btn btn-primary" asp-area="Vendor" asp-controller="Maps" asp-action="Details" asp-route-id="@Model.MapId">
                        @Model.MapName
                    </a>
                </b>
            </p>
            <p>
                Game Mode
                <b>
                    <a class="btn btn-secondary"asp-area="Vendor" asp-controller="GameModes" asp-action="Details" asp-route-id="@Model.GameModeId">
                        @Model.GameModeName
                    </a>
                </b>
            </p>
        </div>
    </div>
    <p>
        Bets for Red Team
        <b>
            @Model.TeamRedBets
        </b>

    </p>
    <p>
        Bets for Blue Team
        <b>
            @Model.TeamBlueBets
        </b>
    </p>
    <span>
        <div class="container">
            <h2>Red Team : @Model.TeamRedName</h2>
            @foreach (var player in Model.TeamRedPlayers)
            {
                <a class="btn" asp-controller="Players" asp-action="Details" asp-route-id="@player.Id">
                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title">@player.UserName</h5>

                            <p class="card-text">@player.SkillLevel</p>
                        </div>
                    </div>
                </a>
            }
        </div>
        <div class="container">
            <h2>Blue Team: @Model.TeamBlueName</h2>
            @foreach (var player in Model.TeamBluePlayers)
            {
                <a class="btn" asp-controller="Players" asp-action="Details" asp-route-id="@player.Id">

                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title">@player.UserName</h5>

                            <p class="card-text">@player.SkillLevel</p>
                        </div>
                    </div>
                </a>
            }
        </div>
    </span>
</div>
</div>*@