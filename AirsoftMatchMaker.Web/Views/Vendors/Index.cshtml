@using System.Text.RegularExpressions;
@model IEnumerable<VendorListModel>
<table class=" table table-bordered  table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Username
            </th>
            <th>
                Ammo Boxes for sale
            </th>
            <th>
                Clothes for Sale
            </th>
            <th>
                Weapons for sale
            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td width="50%">
                    @user.UserName
                </td>
                <td width="30%">
                    @user.AmmoBoxesForSaleCount
                </td>
                <td width="30%">
                    @user.ClothesForSaleCount
                </td>
                <td width="30%">
                    @user.WeaponsForSaleCount
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Vendors" asp-action="Details" asp-route-id="@user.Id" class="btn btn-primary mx-2">
                            <i class="bi bi-info-square-fill"></i>
                            Details
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>

</table>
