@model IEnumerable<WeaponListModel>
@if (Model.Count() == 0)
{
    <h2>No weapons meet the current criteria!</h2>
}
else
{
    <div class="row">
        @foreach (var weapon in Model)
        {
            <partial name="_WeaponPartial" model="@weapon" />
        }
    </div>
}