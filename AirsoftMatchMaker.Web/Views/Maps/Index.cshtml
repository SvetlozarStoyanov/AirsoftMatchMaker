@model IEnumerable<MapListModel>
@if (Model.Count() == 0)
{
    <h2>No maps meet the current criteria!</h2>
}
else
{
    <div class="row">
        @foreach (var map in Model)
        {
            <partial name="_MapPartial" model="@map" />
        }
    </div>
}