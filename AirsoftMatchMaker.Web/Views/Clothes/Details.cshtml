@using System.Text.RegularExpressions;
@model ClothingViewModel

@{
    ViewBag.Title = "Clothing Details";
}

<h2 class="text-center">@ViewBag.Title</h2>


<div class="container" style="display:inline">
    <div class="row">
        @*   <div class="col-4">
        <img class="card-img-top" style="width: 20rem;"
        src="@Model.ImageUrl" alt="House Image">
        </div>*@
        <div class="card col-8 border-0">
            <p style="font-size:25px;">@Model.Name</p>
            @*<p>Located in: <b>@Model.Address</b></p>*@
            <p>
                Description: @Model.Description
            </p>
            <p>
                Color:
                <b>@string.Join(" ", Regex.Matches(Model.ClothingColor.ToString(), "[A-Z][a-z]*").Select(m => m.Value).ToArray())</b>
            </p>
            
            @if (Model.VendorName != null)
            {
                <p>
                    Vendor:
                    <b>
                        <a class="btn btn-primary btn-info" asp-controller="Vendors" asp-action="Details" asp-route-id="@Model.VendorId.Value"> @Model.VendorName</a>
                    </b>
                </p>
            }
            @if (Model.PlayerName != null)
            {
                <p>
                    Player:
                    <b>
                        <a class="btn btn-primary btn-info" asp-controller="Players" asp-action="Details" asp-route-id="@Model.PlayerId.Value"> @Model.PlayerName</a>
                    </b>
                </p>
            }
            <p>
                Price:
                <b>
                    @Model.Price credits
                </b>
            </p>
            @*            <div class="form-inline">
            @if (this.User.Identity.IsAuthenticated)
            {
            <a class="btn btn-warning" asp-controller="Houses" asp-action="Edit"
            asp-route-id="@Model.Id">Edit</a>
            <a class="ml-2 btn btn-danger" asp-controller="Houses" asp-action="Delete"
            asp-route-id="@Model.Id">Delete</a>
            @if (!Model.IsRented)
            {
            <form class="ml-2" asp-controller="Houses"
            asp-action="Rent" asp-route-id="@Model.Id" method="post">
            <input class="btn btn-primary" type="submit" value="Rent" />
            </form>
            }
            else
            {
            <form class="ml-2" asp-controller="Houses" asp-action="Leave"
            asp-route-id="@Model.Id" method="post">
            <input class="btn btn-primary" type="submit" value="Leave" />
            </form>
            }
            }
            </div>*@
        </div>
    </div>
</div>