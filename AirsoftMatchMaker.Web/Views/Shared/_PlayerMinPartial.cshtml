@model PlayerMinModel

<div class="col-md-4 mt-3">
    <div class="card mb-3 bg-dark bg-opacity-10">

        <div class="card-body text-center bg-white bg-opacity-50">
            <h4 class="text-center">@Model.UserName</h4>
            <h6><b>@Model.PlayerClassName</b></h6>
            <br />
            <a asp-controller="Players" asp-action="Details" asp-route-id="@Model.Id"
               class="btn btn-primary">
                Details
            </a>
        </div>
    </div>
</div>
